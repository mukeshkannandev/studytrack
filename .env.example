# StudyTrack Environment Configuration Example
# AI Assistant Mode: 'mock' (default, offline deterministic) or 'real' (optional paid LLM integration)
AI_MODE=mock

# Optional API Key (only used if AI_MODE=real)
# AI_API_KEY=your_api_key_here
